{
  "name": "forgo",
  "version": "4.0.9",
  "type": "module",
  "author": "Jeswin Kumar<jeswinpk@agilehead.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/forgojs/forgo"
  },
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "devDependencies": {
    "@types/jsdom": "^20.0.0",
    "@types/mocha": "^9.1.1",
    "@types/should": "^13.0.0",
    "@types/source-map-support": "^0.5.6",
    "@typescript-eslint/eslint-plugin": "^5.49.0",
    "@typescript-eslint/parser": "^5.49.0",
    "esbuild": "^0.15.5",
    "eslint": "^8.32.0",
    "jsdom": "^20.0.0",
    "mocha": "^10.0.0",
    "rimraf": "^3.0.2",
    "should": "^13.2.3",
    "source-map-support": "^0.5.21",
    "typescript": "^4.9.4"
  },
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "npm run clean && npx tsc && npm run esbuild-make-dev && npm run esbuild-make",
    "build-dev": "npm run clean && npx tsc && npm run esbuild-make-dev",
    "esbuild-make": "npx esbuild ./src/index.ts --minify --bundle --format=esm --target=es2020 --outfile=dist/forgo.min.js",
    "esbuild-make-dev": "npx esbuild ./src/index.ts --bundle --format=esm  --sourcemap --target=es2020 --outfile=dist/forgo.js",
    "test": "npx tsc && npx mocha dist/test/test.js"
  },
  "license": "MIT"
}
